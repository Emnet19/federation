# Chapa payment gateway secret key (server-side only, never expose to the browser).
# Get it from the Chapa dashboard: https://dashboard.chapa.co/
# Use the test key (CHASECK_TEST-...) while developing and the live key for production.
CHAPA_SECRET_KEY=
